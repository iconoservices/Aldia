# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Secretos / tokens de API (Notion, etc.) - el repo es publico
.env
.env.*
!.env.example
firebase-service-account.json
*firebase-adminsdk*.json

# Backups con datos personales reales (tareas, finanzas, proyectos).
# El repo es publico: estos volcados NO deben subirse.
backups/*.json

# Copias manuales de codigo previas a cambios grandes.
# El historial de git ya cumple esa funcion, no hace falta duplicarlo en el repo.
backups/pre-*/
